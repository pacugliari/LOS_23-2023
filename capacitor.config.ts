import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.los_23-2023.app',
  appName: 'LOS 23',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
